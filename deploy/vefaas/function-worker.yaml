# =============================================================
# DEPRECATED — worker is now consolidated into manhuaju-api.
#
# Reason: VeFaaS Micro tier gives ~4 GB total memory quota. The API alone uses
# 4 GB, leaving no room for a dedicated worker function (every release attempt
# failed with `available resources (0) < function MemoryMB`).
#
# Replacement: the API function exposes POST /v1/internal/worker/tick and the
# VeFaaS timer trigger (every 1 minute) drives the queue via that endpoint.
# See `deploy/vefaas/provision.py::ensure_timer(api_fid, ..., path=...)`.
#
# This file is kept (empty) only as a marker. It is not consumed by any tool.
# =============================================================
